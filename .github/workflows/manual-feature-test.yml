name: Manually build feature test
on: 
  workflow_dispatch:
    inputs:
      with_high_priority:
        type: boolean
        description: Build with high priority or not
jobs:
  build-feature-test:
    uses: ./.github/workflows/build-feature-test.yml
