import SwiftUI

struct StrigaTransferView: View {
    var body: some View {
        Text("Transfer")
            .padding(.vertical, 200)
    }
}

struct StrigaTransferView_Previews: PreviewProvider {
    static var previews: some View {
        StrigaTransferView()
    }
}
