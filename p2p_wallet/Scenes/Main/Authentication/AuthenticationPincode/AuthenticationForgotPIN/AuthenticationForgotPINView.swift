import SwiftUI

struct AuthenticationForgotPINView: View {
    //    @Environment(\.presentationMode) private var presentationMode
    
    var body: some View {
        VStack {
            // Forgot PIN view content
        }
        //        .onTapGesture {
        //            presentationMode.wrappedValue.dismiss()
        //        }
    }
}
